using Autofac;
using Core.Autofac;

namespace EmployeeMgt.API
{
    public class EmployeeMgtApiModule : AppModule
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
