namespace Infrastructure.Kernel.Result
{
    public enum ResultStatus
    {
        Ok,
        Error,
        Forbidden,
        Unauthorized,
        Invalid,
        NotFound,
        Conflict,
        Unavailable
    }
}
