namespace SharedKernel.Kernel.Result
{
    public enum ResultStatus
    {
        Ok,
        Error,
        Forbidden,
        Unauthorized,
        Invalid,
        NotFound,
        Conflict,
        Unavailable
    }
}
