namespace Kernel.Result
{
    public enum AppStatusCode
    {
        Ok,
        Error,
        Forbidden,
        Unauthorized,
        Invalid,
        NotFound,
        Conflict,
        Unavailable
    }
}
